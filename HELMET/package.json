{
  "name": "helmet",
  "version": "0.0.1",
  "description": "A destiny 2 news extension that combines all the relevant news sources into one centralized space.\"",
  "main": "dist/tabs.js",
  "scripts": {
    "start": "tsc && node dist/tabs.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.1.3"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.237",
    "@types/node": "^20.2.5",
    "chrome-types": "^0.1.199"
  }
}
